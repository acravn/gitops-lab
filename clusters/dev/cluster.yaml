cluster:
  name: cluster-dev
  # kind - update to EKS endpoint when using EKS
  server: https://kubernetes.default.svc:6443
  environment: dev
  region: us-east-1
  wave: "1"
  features:
    test-app: true
    rollouts: true
